<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.rit.cxc9401.permissiongranted"
    android:versionCode="2"
    android:versionName="Target Version Upgrade">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FormsActivity"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewForm"
            android:parentActivityName=".FormsActivity" />
        <activity android:name=".ViewSignature"
            android:parentActivityName=".SignaturesActivity"
            />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".SignaturesActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".AddEditForm"
            android:label="@string/title_activity_new_form" />
    </application>
</manifest>